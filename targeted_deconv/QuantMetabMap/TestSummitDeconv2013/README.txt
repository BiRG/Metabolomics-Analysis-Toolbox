This directory contains files from the experiments used to evaluate
the Summit-focused deconvolution starting point in 2013. These require
that the path be set up correctly, so before running anything in this
directory (or even using the cell mode "print report" functions, you
must execute "setup_path.m"



